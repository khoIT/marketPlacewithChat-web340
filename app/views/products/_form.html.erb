<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.text_field :name, id:"title", placeholder: "Product Title" %><br>
  <%= f.text_field :price, id:"price", placeholder: "Price" %><br>
  <%= f.text_field :img_url, id:"img_url", placeholder: "Image Url" %><br>
  <%= f.select(:category_id, Category.all.map {|cat| [cat.name, cat.id]}, prompt: 'Choose Category') %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
